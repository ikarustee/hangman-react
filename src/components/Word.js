import React from "react";

export default function Word({ selectedWord, correctLetters }) {
  const checkIfLetterIsPresent = () => {
    selectedWord.split("");
  };
  return <div>{checkIfLetterIsPresent()}</div>;
}

// selectedWord
